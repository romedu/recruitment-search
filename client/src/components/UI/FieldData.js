import React from "react";

const FieldData = ({title, description}) => (
    <div>
        <h6>
            {title}
        </h6>
        <div>
            {description}
        </div>
    </div>
);

export default FieldData;